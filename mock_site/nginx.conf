server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;
    # Preserve the caller's mapped host port on directory-slash redirects.
    absolute_redirect off;
    location / { try_files $uri $uri/ =404; }
}
